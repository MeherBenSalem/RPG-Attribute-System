# RPG Attribute System v3.0.1

## Fixed
- Fixed /ras lock and /ras unlock so they now correctly lock and unlock attributes.
- Fixed level/attribute lock state not updating in-game.
- Fixed attributes GUI hotkey conflict that could open JEI at the same time.
- Fixed player skills and attributes resetting on death when death reset is disabled in config.

## Notes
- Default keybind for opening the attributes menu is now K (can still be changed in Controls).
